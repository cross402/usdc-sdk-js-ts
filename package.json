{
  "name": "@agent-tech/pay",
  "version": "0.1.0",
  "description": "TypeScript client for the Agent Tech v2 payment API",
  "type": "module",
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/esm/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/esm/index.d.ts",
        "default": "./dist/esm/index.js"
      },
      "require": {
        "types": "./dist/cjs/index.d.ts",
        "default": "./dist/cjs/index.js"
      }
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "bun build ./src/index.ts --outdir dist/esm --format esm --target node --packages external && bun build ./src/index.ts --outdir dist/cjs --format cjs --target node --packages external && tsc -p tsconfig.build.json && tsc -p tsconfig.build.cjs.json && node -e \"require('fs').writeFileSync('dist/cjs/package.json', JSON.stringify({type:'commonjs'}))\"",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "license": "MIT",
  "sideEffects": false,
  "devDependencies": {
    "@types/node": "^25.3.1",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@types/bun": "^1.3.9",
    "camelcase-keys": "^10.0.2",
    "decamelize-keys": "^2.0.1"
  }
}
